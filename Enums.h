#pragma once
enum EventType {
	EVENT_COLLISION,
	EVENT_WALL_COLLISON,
	NUM_TYPES
};

enum ComponentID {
	TRANSFORM_COMPONENT,
	DRAW_COMPONENT,
	COLLISION_COMPONENT,
	USER_INPUT_COMPONENT,
	PHYSICS_COMPONENT,
	PLAYER_COMPONENT,
	LOSE_COMPONENT,
	NUM_COMPONENTS,
};

enum WallLocation {
	WALL_TOP,
	WALL_BOTTOM,
	WALL_LEFT,
	WALL_RIGHT
};
