#pragma once
enum EventType {
	EVENT_COLLISION,
	EVENT_WALL_COLLISON,
	NUM_EVENT_TYPES
};

enum ComponentID {
	COMPONENT_TRANSFORM,
	COMPONENT_DRAW,
	COMPONENT_COLLISION,
	COMPONENT_PHYSICS,
	COMPONENT_PLAYER,
	COMPONENT_LOSE,
	COMPONENT_NUM_TYPES,
};

enum WallLocation {
	WALL_TOP,
	WALL_BOTTOM,
	WALL_LEFT,
	WALL_RIGHT
};
