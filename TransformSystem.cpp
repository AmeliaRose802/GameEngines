#include "TransformSystem.h"


void TranformSystem::update(float deltaTime) {
	/*
	std::map<int, Component *>::iterator it;
	for (it = components.begin(); it != components.end(); ++it)
		it->second->update();
		*/
}

